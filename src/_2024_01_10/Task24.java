package _2024_01_10;

import java.util.Arrays;
import java.util.Collection;
import java.util.Collections;
import java.util.List;
import java.util.stream.Collectors;
import java.util.stream.Stream;

public class Task24 {
    public static void main(String[] args) {
        //Перемешать элементы списка.
       /* List<Character> characterList = List.of('A', 'B', 'C', 'D', 'C', 'A');
        System.out.println(shuffleList(characterList));

        List<Float> floatList = Arrays.asList(1.2f, 2.4f, 3.6f, 4.8f, 6.0f, 7.2f, 2.4f);
        System.out.println(shuffleList(floatList));

        List<Integer> integerList = List.of(1, 2, 3, 4, 5, 6, 3, 2);
        System.out.println(shuffleList(integerList));

        List<String> stringList = List.of("one", "two", "three", "four", "twiks", "town", "one", "four");
        System.out.println(shuffleList(stringList));*/

    }

   /* static <T> List<T> shuffleList(List<T> input) {
        List<T> list = new java.util.ArrayList<>(input.stream()
                .toList());

        return input.stream()
                .map(Collections.shuffle(list))
                .toList();

    }*/
}
